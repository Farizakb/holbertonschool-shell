#!/usr/bin/env bash
# Displays the PID and process name of processes containing "bash"
for pid in /proc/*/; do
    if [ -f "$pid/comm" ]; then
        name=$(cat "$pid/comm")
        if [[ $name == *bash* ]]; then
            echo "$(basename "$pid") $name"
        fi
    fi
done
